package com.techblog.common.validation;

/**
 * Custom Validation
 * Package chứa các custom validator annotations và implementations.
 *
 * Ví dụ: @ValidEmail, @StrongPassword, @UniqueUsername...
 *
 * TODO: Tạo custom annotations khi cần:
 * - ValidEmail.java (annotation)
 * - EmailValidator.java (implementation)
 * - StrongPassword.java (annotation)
 * - PasswordValidator.java (implementation)
 */
public class ValidationMarker {
    // Marker class - sẽ được thay thế bằng các custom validators
}
