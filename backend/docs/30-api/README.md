# API Documentation

Chứa đặc tả tài liệu kiến trúc RESTful Endpoints. 

> Vui lòng tham khảo kỹ file `api-conventions.md` để nắm được định dạng JSON response mà toàn bộ backend trả về trước khi xử lý.

Thư mục có bao gồm file export `postman-collection.json` dùng để dễ dàng Test / Validate hoạt động các luồng gọi.
